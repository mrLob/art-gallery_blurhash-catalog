import styled from 'styled-components';

export const Wrapper = styled.span`
  display: inline-block;
  width: 100%;
  text-align: center;
  margin: auto 0;
  font-size: 2.5rem;
  font-weight: 300;
  font-style: italic;
  opacity: 0.5;
`;
