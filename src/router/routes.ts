export enum Paths {
  paints = '/',
  paintByYear = '/:year',
  paint = '/paints/:slug',
  prints = '/prints',
  print = '/prints/:slug',
  printsByCategory = '/prints/category/:category',
  about = '/about',
}
